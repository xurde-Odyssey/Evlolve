# Supabase

This directory is reserved for future Supabase clients, server helpers, and typed database integration.

Do not add credentials to the repository. Use `.env.local` for local values and keep `.env.example` current.
